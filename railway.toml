[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -c 'npx prisma db push --accept-data-loss || echo skip-push; npx prisma db seed || echo skip-seed; npm start'"
healthcheckPath = "/api/health"
healthcheckTimeout = 180
restartPolicyType = "on_failure"
